# Copy to .env and fill in real values

DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/unilearn?schema=public"

JWT_ACCESS_SECRET="at-least-32-random-chars"
JWT_REFRESH_SECRET="at-least-32-different-random-chars"

PORT=3000
CORS_ORIGIN=http://localhost:8081

SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM="UniLearn <noreply@unilearn.al>"
