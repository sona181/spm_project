# Copy this to .env.local and fill in your values
# Expo reads EXPO_PUBLIC_* variables at build time

# NestJS backend base URL — include /api prefix, no trailing slash
EXPO_PUBLIC_API_URL=http://192.168.88.251:3000/api
